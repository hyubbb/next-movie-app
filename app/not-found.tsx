import { Metadata } from "next";

export const metadata: Metadata = { title: "not found!" };

const NotFound = () => {
  return (
    <div>
      <h1>not found</h1>
    </div>
  );
};

export default NotFound;
